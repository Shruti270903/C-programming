#include<stdio.h>
int main()
{
    int x,y, sum;
    printf("enter the value of x :\n");
    scanf("%d", &x);
    printf("enter the value of y :\n");
    scanf("%d", &y);
    sum = x + y;
    printf("sum of x+y: %d", sum);
    return 0;
}